# Super_AstrBot 依赖声明
#
# 设计取舍：**尽量零依赖**。
#
# - 持久化使用 Python 标准库 `sqlite3`（配合 asyncio.to_thread），不引入 aiosqlite；
#   这样在任何精简环境下都能装上即用，且只有一条代码路径。
# - 中文分词优先使用宿主 AstrBot 已内置的 `jieba`；缺失时自动降级为字符 bigram，
#   因此这里只作为「可选增强」声明，不声明也不影响插件运行。
#
# 如果确实需要更精确的中文分词，可保留下面这行（宿主通常已自带，pip 会跳过安装）：
jieba>=0.42.1
